import React, { useState } from 'react';\nimport {\n  View,\n  Text,\n  StyleSheet,\n  TextInput,\n  FlatList,\n  TouchableOpacity,\n  SafeAreaView,\n} from 'react-native';\nimport { MaterialCommunityIcons } from '@expo/vector-icons';\n\nconst SearchScreen = ({ notes = [], voiceNotes = [], navigation }) => {\n  const [searchQuery, setSearchQuery] = useState('');\n  const [activeTab, setActiveTab] = useState('all'); // all, text, voice\n\n  // Combine and filter notes\n  const allNotes = [\n    ...notes.map(note => ({ ...note, type: 'text' })),\n    ...voiceNotes.map(note => ({ ...note, type: 'voice' })),\n  ];\n\n  const filteredNotes = allNotes.filter(note => {\n    const matchesSearch =\n      note.title.toLowerCase().includes(searchQuery.toLowerCase()) ||\n      (note.content && note.content.toLowerCase().includes(searchQuery.toLowerCase()));\n\n    if (activeTab === 'text') return matchesSearch && note.type === 'text';\n    if (activeTab === 'voice') return matchesSearch && note.type === 'voice';\n    return matchesSearch;\n  });\n\n  const renderNoteCard = ({ item }) => (\n    <TouchableOpacity\n      style={styles.noteCard}\n      onPress={() => {\n        if (item.type === 'voice') {\n          navigation.navigate('VoiceNotes', {\n            screen: 'VoiceDetail',\n            params: { note: item },\n          });\n        } else {\n          navigation.navigate('TextNotes', {\n            screen: 'NoteDetail',\n            params: { note: item },\n          });\n        }\n      }}\n    >\n      <View style={styles.noteHeader}>\n        <MaterialCommunityIcons\n          name={item.type === 'voice' ? 'microphone' : 'note-text'}\n          size={20}\n          color={item.type === 'voice' ? '#667eea' : '#764ba2'}\n        />\n        <Text style={styles.noteTitle}>{item.title}</Text>\n        <View style={styles.typeBadge}>\n          <Text style={styles.typeBadgeText}>\n            {item.type === 'voice' ? '🎤' : '📝'}\n          </Text>\n        </View>\n      </View>\n      <Text style={styles.notePreview} numberOfLines={2}>\n        {item.content || 'Voice note'}\n      </Text>\n      <Text style={styles.noteDate}>{item.date}</Text>\n    </TouchableOpacity>\n  );\n\n  return (\n    <SafeAreaView style={styles.container}>\n      {/* Search Bar */}\n      <View style={styles.searchContainer}>\n        <MaterialCommunityIcons name=\"magnify\" size={24} color=\"#667eea\" />\n        <TextInput\n          style={styles.searchInput}\n          placeholder=\"Search notes...\"\n          value={searchQuery}\n          onChangeText={setSearchQuery}\n          placeholderTextColor=\"#999\"\n        />\n        {searchQuery.length > 0 && (\n          <TouchableOpacity onPress={() => setSearchQuery('')}>\n            <MaterialCommunityIcons name=\"close\" size={24} color=\"#999\" />\n          </TouchableOpacity>\n        )}\n      </View>\n\n      {/* Filter Tabs */}\n      <View style={styles.filterTabs}>\n        <TouchableOpacity\n          style={[\n            styles.filterTab,\n            activeTab === 'all' && styles.activeFilterTab,\n          ]}\n          onPress={() => setActiveTab('all')}\n        >\n          <Text\n            style={[\n              styles.filterTabText,\n              activeTab === 'all' && styles.activeFilterTabText,\n            ]}\n          >\n            All ({allNotes.length})\n          </Text>\n        </TouchableOpacity>\n\n        <TouchableOpacity\n          style={[\n            styles.filterTab,\n            activeTab === 'text' && styles.activeFilterTab,\n          ]}\n          onPress={() => setActiveTab('text')}\n        >\n          <Text\n            style={[\n              styles.filterTabText,\n              activeTab === 'text' && styles.activeFilterTabText,\n            ]}\n          >\n            📝 Text ({notes.length})\n          </Text>\n        </TouchableOpacity>\n\n        <TouchableOpacity\n          style={[\n            styles.filterTab,\n            activeTab === 'voice' && styles.activeFilterTab,\n          ]}\n          onPress={() => setActiveTab('voice')}\n        >\n          <Text\n            style={[\n              styles.filterTabText,\n              activeTab === 'voice' && styles.activeFilterTabText,\n            ]}\n          >\n            🎤 Voice ({voiceNotes.length})\n          </Text>\n        </TouchableOpacity>\n      </View>\n\n      {/* Results */}\n      {filteredNotes.length > 0 ? (\n        <FlatList\n          data={filteredNotes}\n          keyExtractor={(item) => item.id.toString()}\n          renderItem={renderNoteCard}\n          contentContainerStyle={styles.listContent}\n        />\n      ) : (\n        <View style={styles.emptyContainer}>\n          <MaterialCommunityIcons name=\"magnify\" size={48} color=\"#999\" />\n          <Text style={styles.emptyText}>\n            {searchQuery\n              ? `No notes found for \"${searchQuery}\"`\n              : 'Start typing to search your notes'}\n          </Text>\n        </View>\n      )}\n    </SafeAreaView>\n  );\n};\n\nconst styles = StyleSheet.create({\n  container: {\n    flex: 1,\n    backgroundColor: '#f5f5f5',\n  },\n  searchContainer: {\n    flexDirection: 'row',\n    alignItems: 'center',\n    backgroundColor: 'white',\n    marginHorizontal: 10,\n    marginVertical: 10,\n    paddingHorizontal: 15,\n    borderRadius: 10,\n    borderWidth: 2,\n    borderColor: '#e0e0e0',\n  },\n  searchInput: {\n    flex: 1,\n    paddingVertical: 12,\n    marginHorizontal: 10,\n    fontSize: 16,\n    color: '#333',\n  },\n  filterTabs: {\n    flexDirection: 'row',\n    paddingHorizontal: 10,\n    paddingVertical: 10,\n    gap: 8,\n  },\n  filterTab: {\n    flex: 1,\n    paddingVertical: 8,\n    paddingHorizontal: 12,\n    borderRadius: 20,\n    backgroundColor: 'white',\n    borderWidth: 2,\n    borderColor: '#e0e0e0',\n  },\n  activeFilterTab: {\n    backgroundColor: '#667eea',\n    borderColor: '#667eea',\n  },\n  filterTabText: {\n    textAlign: 'center',\n    fontSize: 12,\n    fontWeight: '600',\n    color: '#666',\n  },\n  activeFilterTabText: {\n    color: 'white',\n  },\n  listContent: {\n    padding: 10,\n  },\n  noteCard: {\n    backgroundColor: 'white',\n    borderRadius: 10,\n    padding: 15,\n    marginBottom: 12,\n    shadowColor: '#000',\n    shadowOffset: { width: 0, height: 2 },\n    shadowOpacity: 0.1,\n    shadowRadius: 4,\n    elevation: 3,\n  },\n  noteHeader: {\n    flexDirection: 'row',\n    alignItems: 'center',\n    marginBottom: 10,\n  },\n  noteTitle: {\n    flex: 1,\n    fontSize: 16,\n    fontWeight: '600',\n    color: '#333',\n    marginLeft: 10,\n  },\n  typeBadge: {\n    paddingHorizontal: 8,\n    paddingVertical: 4,\n    backgroundColor: '#f0f0f0',\n    borderRadius: 6,\n  },\n  typeBadgeText: {\n    fontSize: 12,\n  },\n  notePreview: {\n    fontSize: 14,\n    color: '#666',\n    lineHeight: 20,\n    marginBottom: 8,\n  },\n  noteDate: {\n    fontSize: 12,\n    color: '#999',\n  },\n  emptyContainer: {\n    flex: 1,\n    justifyContent: 'center',\n    alignItems: 'center',\n  },\n  emptyText: {\n    fontSize: 16,\n    color: '#999',\n    textAlign: 'center',\n    marginTop: 15,\n  },\n});\n\nexport default SearchScreen;\n